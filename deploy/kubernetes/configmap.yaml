apiVersion: v1
kind: ConfigMap
metadata:
  name: statuspulse
  namespace: statuspulse
data:
  STATUSPULSE_ADDR: ":8080"
  STATUSPULSE_DB_PATH: "/data/statuspulse.db"
  STATUSPULSE_CHECK_INTERVAL: "1m"
  STATUSPULSE_REQUEST_TIMEOUT: "5s"
