apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: statuspulse-data
  namespace: statuspulse
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Use the cluster's default StorageClass.
